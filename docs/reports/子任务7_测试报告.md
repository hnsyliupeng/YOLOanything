# 子任务7 测试报告 · 视频文件 + 实时摄像头

**日期**：2026-09-24　**结论**：✅ 视频模式 5/5 自动化通过；摄像头完整实现（沙箱无设备，按约定不实测）

## 视频文件模式（实测）

**测试素材**：`app/assets/samples/test_water.webm`（824KB，480×360，4s@8fps，val 帧拼接+水平漂移模拟水流）
- 生成时发现：**mp4v 编码 headless Chromium 不支持**（打开静默失败）→ 改 VP8/WebM（Chrome 原生编解码）

**自动化**（`tests/subtask7.mjs`，puppeteer 真点击流）：**5/5 通过**
| 断言 | 结果 |
|---|---|
| 视频打开并抽帧 | ✅ |
| 帧循环连续推理（≥3 帧） | ✅ count=5 |
| 媒体类型 video-frame | ✅ |
| 视频控件条显示（暂停/自动推理/间隔/状态） | ✅ `test_water.webm 480×360 · 4s` |
| 零页面错误 | ✅ |

**实现**（app/js/ui/video.js）：
- `video:open` 事件接入（media.js 的 loadFile 对 video/* 已预留）
- 帧循环按可调间隔（0.3-3s，默认 1.2s）`createImageBitmap(video)` → `media.setBitmap` → 复用图像模式完整 infer:run 管线（检测+mask+SMA3+深度+统计+预警）
- 暂停/播放联动帧循环；模式切换 Tab 时自动停循环/关摄像头

## 实时摄像头模式（实现完整，未实测）

- `getUserMedia`：设备枚举下拉、facingMode=environment 优先、理想 1280×720
- 权限拒绝/无设备的 toast 提示与状态栏反馈完整
- **沙箱无摄像头**（用户约束：实现但暂不测试）——代码路径与视频模式共用帧循环，风险低

## 性能

- 视频模式单帧端到端 ~585ms（检测 320 wasm，无深度）；帧率受 SwiftShader 影响 15fps（真实设备 WebGPU 下预计 25-30fps+）

## 截图

- `docs/screenshots/subtask7/r1_视频模式.png`：视频帧+深度小窗+底部控件条+统计面板

**结论**：视频连续推理链路验收通过；摄像头留待用户环境验证
