# 子任务9 测试报告 · 训练面板 + 全系统联调

**日期**：2026-09-24　**结论**：✅ 训练面板实测通过；九大子任务全链联调完成

## 训练面板（app/js/ui/train.js）

- **训练历史**：fetch `./training-history.json`（plot_curves.py 同步产出）→ 轮次表（配置/box mAP50/mask mAP50）+ 每 epoch 迷你 sparkline 曲线
- **超参表单**：model/epochs/imgsz/batch/optimizer/lr0/fraction → 实时生成 ultralytics CLI 命令 → 一键复制（含 execCommand fallback）
- **实测**：`rows=1（R6: 4ep×40%@320 SGD · 0.0047/0.0060）、sparks=1、cmd="python -m ultralytics yolo segment train model=yolo26n-seg.pt …"`、面板随 Tab 显隐、CSS 限高滚动防溢出
- 沙箱 CPU 无法承载完整训练的说明与 `scripts/train_gpu.yaml`（150ep@640 copy_paste0.3）指引直接内嵌面板

## 全系统联调清单（本轮自动化实测汇总）

| 子任务 | 验证 | 结果 |
|---|---|---|
| ① 骨架+WebGPU | boot/渲染/FPS/回退 | ✅（53/53 历史回归 + 本轮 Canvas2D 熔断路径复验） |
| ② 数据集 | 5328/502/178/eval + syn_depth 4000/200 | ✅（第4次重置后自愈脚本重建复验） |
| ③ 检测+分割推理 | subtask3 R4 | ✅ **16/16** |
| ④ 深度 | DAV2 web + DepthLite 双轨 | ✅ 11/11 目标有深度值 |
| ⑤ 关系+SMA3 | 关系图/SMA3 | ✅ 11节点36边/3簇/10增强 |
| ⑥ 图像+ROI | 全功能+ROI | ✅ ROI 过滤+可视化 |
| ⑦ 视频+摄像头 | 视频 5/5 | ✅（摄像头按约定未测） |
| ⑧ 预警+导出 | 预警/JSON/CSV | ✅（修导出按钮真 bug） |
| ⑨ 训练面板 | 历史/CLI/复制 | ✅ |

**联调要点修复**（全部在本轮真实暴露）：
1. GPU 设备丢失风暴 → 8 次熔断换画布回退 Canvas2D
2. WebGPU 会话"加载成功、推理失败" → 推理期降级 WASM 重试
3. SwiftShader 画布黑表面 → willReadFrequently 软件后备 + 自愈重建
4. 导出按钮永禁用 → 首记录启用
5. `media:clear` 无限递归风险 → 事件流向梳理
6. renderDetections 拿到的是 ROI/增强过滤前的 result.dets 引用 → 传 `dets` 变量

## 已知限制（诚实记录）

- 沙箱 2 核 SwiftShader：推理 0.6-1.6s/帧、深度 3.4-5.4s——用户端 WebGPU 硬件将快一个量级
- R6 从零 4ep 模型召回低（mAP50=0.0047）：联调测试统一 conf=0.05 口径；高精度需用户 GPU 按 train_gpu.yaml 训练
- 摄像头模式沙箱无法端到端实测

## 截图

- `docs/screenshots/subtask9/训练面板.png`：训练历史表+sparkline+超参表单+CLI 命令（含导出 toast 入镜）

**结论**：九大子任务全部按 Loop Hardness 流程验收通过，系统可交付
