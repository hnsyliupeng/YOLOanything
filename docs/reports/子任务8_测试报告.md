# 子任务8 测试报告 · 事件预警 + 记录导出 + 高级功能

**日期**：2026-09-24　**结论**：✅ 预警（视觉/声音/事件列表）+ JSON/CSV 导出全部实测通过

## 事件预警（app/js/ui/alerts.js）

**规则**（阈值右侧面板实时可调）：
- 高密度：垃圾类目标数 ≥ 阈值（默认 8，测试降到 1/2 触发）
- 大型漂浮物：单目标面积占画面 ≥ 阈值%（默认 25）

**触发链**：evaluateAlerts(dets, dim) → 事件列表 prepend（时间+文案）→ stat-alerts 计数 → 视口红色闪烁动画（.alert-flash 0.35s×4）→ 双音下行蜂鸣（880/587Hz，WebAudio 合成，sw-sound 可关）

**实测**（11 目标场景）：
| 项 | 结果 |
|---|---|
| 密度预警触发 | ✅ `垃圾密度超限：9 ≥ 8（阈值可调）`（多次触发计数正确累加 stat-alerts） |
| 事件列表 | ✅ 按时间倒序、上限 30 条、event-item 样式 |
| 视觉闪烁 | ✅（截图 ④ 红框时段） |
| 页面错误 | ✅ 0 |

## 记录导出（app/js/ui/exports.js）

- 每次推理 addRecord：时间/模式/模型/推理ms/ROI/逐目标明细（cls/name/score/box/depth/clusterSize）
- **修复真 bug**：导出按钮 HTML 里 `disabled` 且无启用逻辑——任何用户第一次点击都会无效！修复为首条记录到达即启用
- 验证（blob 拦截，页内真实点击）：
  - JSON：`{records:1, dets:4, hasSettings:true, hasRegion:true}` + settings（thresholds/flags/alert/region）完整
  - CSV：表头 `时间,模式,模型,推理ms,ROI,类别ID,类别,置信度,x,y,w,h,深度,簇大小`，样本行正确，\uFEFF BOM（Excel 中文兼容），无检出时也有占位行

## 高级功能集成状态

- 阈值调节（conf/IoU）实时作用于推理 ✅（subtask3 R4 dets 随 conf 变化验证）
- 模型切换（检测 640/320 + 深度三选）✅ 选择器接线 + 按所选 id 加载
- 简易训练面板 → 见子任务9 报告

## 截图

- `docs/screenshots/subtask8/预警触发_视觉闪烁.png`：事件记录「垃圾密度超限：9 ≥ 8」+ stat-alerts 计数

**结论**：预警与导出验收通过（含 1 个真实用户可见 bug 的发现与修复）
